import React from 'react';
import { List } from './types';
import { Header } from './PropertyDetails.styling';
const PropertyLists = (props: List[]) => {
  return <Header>Before</Header>;
};

export default PropertyLists;
