import React from 'react';
import { Container, LeafletMap } from '../components/index';

const Map = () => {
    return(
        <Container>
            <LeafletMap />
        </Container>
    );
};

export default Map;
